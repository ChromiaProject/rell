module;

entity ba_entity {
    key name;
    data: byte_array;
}

struct ba_struct {
    name;
    data: byte_array;
}

query ba_query(name): ba_struct {
    return ba_entity @ { name } (ba_struct(.name, .data));
}

operation ba_op(ba: ba_struct) {
    create ba_entity(ba.name, ba.data);
}
